import { FC } from 'react';
import { Button, Input, message } from 'antd'; 
import './index.scss';

const Pay: FC = () => {
  return <div className='pay-page'>
    {/* 无畏契约点券充值（固定点券10080）
    <Input placeholder='请输入账号' />
    <Input placeholder='邀请码（必须）' />
    <p>
      100RMB = 10080点券
    </p>
    <p>
      邀请制，邀请码必须填写！
    </p>
    <p>
      点卷来源为黑信用卡充值，充值后无法退回！ 账号被封本店概不负责！
    </p>
    <Button type='primary' onClick={() => message.error('邀请码不正确')}>提交</Button>
    <p style={{marginTop: 20}}>林清威刚刚写的小页面</p> */}
    跑路了 别看了
  </div>
}

export default Pay;